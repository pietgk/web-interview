{
  "name": "web-interview",
  "version": "1.0.0",
  "private": true,
  "description": "Sellpy web interview assignment",
  "scripts": {
    "test": "npm test --prefix shared && npm test --prefix backend && npm test --prefix frontend",
    "test:shared": "npm test --prefix shared",
    "test:backend": "npm test --prefix backend",
    "test:frontend": "npm test --prefix frontend",
    "test:e2e": "playwright test",
    "typecheck": "tsc -p jsconfig.json && tsc -p shared/jsconfig.json && tsc -p backend/jsconfig.json && tsc -p frontend/jsconfig.json",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^24.13.3",
    "@web-interview/todos": "file:shared",
    "typescript": "5.9.3"
  }
}
