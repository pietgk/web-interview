@AGENTS.md

<!--
Deliberately a one-line import, not a copy. The working agreement lives in
AGENTS.md, the vendor-neutral file that every coding agent reads, so it survives
dropping any single tool. Claude Code expands the @import above, so both names
resolve to the same content and there is nothing to keep in sync.

Do not move content here, and do not invert this into AGENTS.md importing
CLAUDE.md: @import is Claude Code syntax, so other agents would read the literal
string and get no instructions at all.
-->
